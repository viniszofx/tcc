import ResetPassword from "@/components/auth/reset-password";

export const metadata = {
  title: "Redefinir senha - KDÊ",
};

export default function ResetPasswordPage() {
  return <ResetPassword />;
}